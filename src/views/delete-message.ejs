<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete Message | membersonly</title>
    <%- include("partials/styles") %>
  </head>
  <body>
    <%- include("partials/header") %>

    <main class="main">
      <h1 class="main__title">Delete Message</h1>

      <div class="delete-confirmation">
        <div class="delete-confirmation__warning">
          <img src="/assets/images/delete.svg" alt="Delete" class="delete-confirmation__icon" />
          <h2 class="delete-confirmation__warning-title">Are you sure you want to delete this message?</h2>
          <p class="delete-confirmation__warning-message">This action cannot be undone.</p>
        </div>

        <div class="delete-confirmation__message">
          <h3 class="delete-confirmation__message-title"><%= message.title %></h3>
          <p class="delete-confirmation__message-content"><%= message.content %></p>
          <div class="delete-confirmation__meta">
            <span>Created: <%= new Date(message.createdAt).toLocaleString() %></span>
          </div>
        </div>

        <form action="/messages/delete/<%= message.id %>" method="post">
          <div class="form__actions">
            <a href="/" class="btn btn--secondary">Cancel</a>
            <button class="btn btn--danger" type="submit">
              <img src="/assets/images/delete.svg" class="btn__icon--delete" alt="Delete" />
              Delete Message
            </button>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
