<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Settings | membersonly</title>
    <%- include("partials/styles") %>
  </head>
  <body>
    <%- include("partials/header") %>

    <main class="main">
      <h2 class="main__title">Account Settings</h2>

      <%- include("partials/errors") %>

      <% if (success) { %>
        <div class="success">
          <div class="success__title">✅ Profile updated successfully</div>
          <p class="success__message">Your profile has been updated successfully</p>
        </div>
      <% } %>

      <form class="form form--settings" action="/settings/update" method="post">
        <div class="form-group">
          <label for="username">
            Username <span class="required" aria-label="required">*</span>
          </label>
          <input 
            type="text" 
            name="username" 
            value="<%= user.username %>" 
            required 
            aria-required="true"
          />
        </div>

        <div class="form-group">
          <label for="firstName">
            First Name <span class="required" aria-label="required">*</span>
          </label>
          <input 
            type="text" 
            name="firstName" 
            value="<%= user.firstName %>" 
            required 
            aria-required="true"
          />
        </div>

        <div class="form-group">
          <label for="lastName">
            Last Name <span class="required" aria-label="required">*</span>
          </label>
          <input 
            type="text" 
            name="lastName" 
            value="<%= user.lastName %>" 
            required 
            aria-required="true"
          />
        </div>

        <div class="form__actions">
          <a class="btn btn--secondary" href="/settings/password">Change Password</a>
          <a class="btn btn--danger" href="/settings/delete">Delete Account</a>
          <button class="btn" type="submit">Update Profile</button>
        </div>
      </form>
    </main>
  </body>
</html>