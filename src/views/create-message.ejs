<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Message | membersonly</title>
    <%- include("partials/styles") %>
  </head>
  <body>
    <%- include("partials/header") %>

    <main class="main">
      <h1 class="main__title">Create Message</h1>

      <form class="form form--create-message" action="/messages/create" method="post">
        <div class="form-group">
          <label for="title">
            Title <span class="required" aria-label="required">*</span>
          </label>
          <input 
            type="text" 
            name="title" 
            required 
            aria-required="true"
          />
        </div>

        <div class="form-group">
          <label for="content">
            Content <span class="required" aria-label="required">*</span>
          </label>
          <textarea 
            name="content" 
            required 
            aria-required="true"
          ></textarea>
        </div>

        <button class="btn" type="submit">Create Message</button>
      </form>
    </main>
  </body>
</html>