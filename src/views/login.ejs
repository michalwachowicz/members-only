<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | membersonly</title>
    <%- include("partials/styles") %>
  </head>
  <body>
    <%- include("partials/header") %>

    <main class="main">
      <h2 class="main__title">Login to your account</h2>

      <%- include("partials/errors") %>

      <form class="form form--login" action="/auth/login" method="post">
        <div class="form-group">
          <label for="username">
            Username <span class="required" aria-label="required">*</span>
          </label>
          <input
            type="text"
            name="username"
            value="<%= typeof formData !== 'undefined' && formData.username ? formData.username : '' %>"
            required
            aria-required="true"
          />
        </div>

        <div class="form-group">
          <label for="password">
            Password <span class="required" aria-label="required">*</span>
          </label>
          <input 
            type="password" 
            name="password" 
            required 
            aria-required="true"
          />
        </div>

        <button class="btn" type="submit">Login</button>
      </form>

      <p class="account-not-member">
        Don't have an account? <a class="link" href="/auth/register">Register here</a>
      </p>
    </main>
  </body>
</html>
